function sum(a: int, b: int) { 
    if(a > b) {
    return a - b; 
    } else if (a < b) {
    return a + b; 
    } else {
    return 'hola';
    }
} 

public class Point { 
    int x = 10; 
    float y = 20.0; 
}; 

Point obj = new Point(); 
obj.x = 5; 

read(obj.y); 
print(obj.x); 
print(sum(obj.x, obj.y)); 
print('hola' + 'como esta');